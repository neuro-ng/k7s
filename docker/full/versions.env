# k7s batteries-included image — pinned tool versions (Phase 38).
#
# Renovate auto-bumps these via the custom regex manager in .github/renovate.json.
# To update manually: edit the version here and rebuild with:
#   docker build --build-arg KUBECTL_VERSION=vX.Y.Z ...

KUBECTL_VERSION=v1.30.14
HELM_VERSION=v3.21.0
VELA_VERSION=v1.10.8
TRIVY_VERSION=v0.71.0
GCLOUD_VERSION=570.0.0
